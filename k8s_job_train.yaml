apiVersion: batch/v1
kind: Job
metadata:
  name: train-job
spec:
  template:
    spec:
      containers:
      - name: train
        image: 9177746670/ml-pipeline:latest
        command: ["python", "train.py"]
      restartPolicy: Never
  backoffLimit: 2
